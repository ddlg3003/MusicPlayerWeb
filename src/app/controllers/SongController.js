const Song = require('../models/Song');

class SongController {
    // [GET] /song/add
    add(req, res) {
        
    }

    // [POST] /song/done
    done(req, res) {

    }
}

module.exports = new SongController();